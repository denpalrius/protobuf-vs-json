syntax = "proto3";

package planets;

message Planet {
    int32 id = 1;
    string name = 2;
    int32 orderFromTheSun = 3;
    bool hasLife = 4;
}

message Planets {
    repeated Planet planets = 1;
}
